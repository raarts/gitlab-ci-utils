#!/bin/bash
set -e

for service in $*
do
  if [ ! -d $service ]
  then
    echo "No directory $service found - exit"
    exit 1
  fi
  cd $service
  [ -x gitlab-ci/test ] && gitlab-ci/test $service
  cd ..
done

