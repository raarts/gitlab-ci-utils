#!/bin/bash
# set -x

. `dirname $0`/gitlab-ci-util.lib

for service in $*
do
  if [ -f $service/gitlab-ci/test ]
  then
    cd $service
    gitlab-ci/test
    cd ..
  else
    echo "$service/gitlab-ci/test not found - skipping test of $service"
  fi
done

